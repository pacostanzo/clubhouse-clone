import React from 'react';
import { PlanLayoutContainer } from './Plan.style';

const PlanLayout = ({ children }) => {
  return <PlanLayoutContainer>{children}</PlanLayoutContainer>;
};
export default PlanLayout;
